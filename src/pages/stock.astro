---
import Layout from "../layouts/Layout.astro";
import StockTable from "../components/StockTable.astro";
import ProductModal from "../components/ProductModal.astro";
import ProductHistoryModal from "../components/ProductHistoryModal.astro";
import { createBackend } from "../lib/convex";
import { isDemoMode } from "../lib/runtime";
import { mockStock, mockProjects } from "../lib/mock-data";
import type { VistaStockFinal } from "../types/database";

const { t } = Astro.locals;

let data: VistaStockFinal[] = [];
let errorMsg = "";
let projects: any[] = [];

// Get Project ID
const projectIdParam = Astro.url.searchParams.get("projectId");
let projectId = projectIdParam ? parseInt(projectIdParam) : null;

if (isDemoMode(Astro.locals)) {
    projects = mockProjects;
    if (!projectId) {
        projectId = mockProjects[0]?.id || null;
    }
    if (projectId) {
        data = mockStock.filter((s) => s.proyecto_id === projectId);
    }
} else {
    const backend = createBackend(Astro.locals, { kind: "session", userId: Astro.locals.user!.tokenIdentifier });
    projects = await backend.listProjects();

    // If no project ID in URL, fetch the first available project to default to
    if (!projectId) {
        projectId = projects[0]?.id ?? null;
    }

    // Fetch data if we have a project ID
    if (projectId) {
        try {
            data = (await backend.listStock({ projectId, page: 1, pageSize: 1000 })).data as unknown as VistaStockFinal[];
        } catch (error) {
            errorMsg = error instanceof Error ? error.message : "Unknown error";
        }
    }
}
---

<Layout title={t("title.stock")} projects={projects}>
    {
        isDemoMode(Astro.locals) && (
            <div class="mb-6 px-4 py-3 rounded-xl bg-amber-500/10 border border-amber-500/20 text-amber-400 text-sm flex items-center gap-3">
                <svg
                    xmlns="http://www.w3.org/2000/svg"
                    class="w-5 h-5 shrink-0"
                    fill="none"
                    viewBox="0 0 24 24"
                    stroke="currentColor"
                >
                    <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="2"
                        d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"
                    />
                </svg>
                <span>
                    <strong>{t("demo.title")}</strong> — {t("demo.short")}
                </span>
            </div>
        )
    }

    <div class="mb-8 flex items-center justify-between">
        <div>
            <h1
                class="text-3xl font-bold bg-clip-text text-transparent bg-linear-to-r from-white to-slate-400"
            >
                {t("stock.title")}
            </h1>
            <p class="text-slate-400 mt-2">
                {t("stock.subtitle")}
            </p>
        </div>

        <div class="flex gap-3">
            <button
                onclick="document.getElementById('modal-producto').showModal()"
                class="px-4 py-2 rounded-lg bg-primary-500 hover:bg-primary-600 text-white font-medium text-sm transition-all shadow-lg shadow-primary-500/20 flex items-center gap-2 cursor-pointer"
            >
                <svg
                    xmlns="http://www.w3.org/2000/svg"
                    class="w-4 h-4"
                    fill="none"
                    viewBox="0 0 24 24"
                    stroke="currentColor"
                >
                    <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="2"
                        d="M12 4v16m8-8H4"></path>
                </svg>
                {t("stock.newProduct")}
            </button>
        </div>
    </div>

    {
        errorMsg && (
            <div class="p-4 mb-6 rounded-xl bg-red-500/10 border border-red-500/20 text-red-400">
                {t("common.errorLoadingData")}: {errorMsg}
            </div>
        )
    }

    <StockTable data={data} />
    <ProductModal />
    <ProductHistoryModal />
</Layout>
