---
import Layout from "../layouts/Layout.astro";
import { isDemoMode } from "../lib/runtime";

// In demo mode, redirect straight to dashboard
if (isDemoMode(Astro.locals)) {
    return Astro.redirect("/");
}

const { t } = Astro.locals;
const showInvalidCredentials = Astro.url.searchParams.get("error") === "invalid_credentials";
---

<Layout title={t("title.login")}>
    <div class="flex items-center justify-center min-h-[80vh]">
        <div
            class="w-full max-w-md p-8 rounded-2xl bg-[#14151a]/50 backdrop-blur-xl border border-white/5 shadow-2xl relative overflow-hidden group"
        >
            <!-- Background Glow -->
            <div
                class="absolute -top-20 -right-20 w-40 h-40 bg-primary-500/20 rounded-full blur-3xl group-hover:bg-primary-500/30 transition-all duration-500"
            >
            </div>
            <div
                class="absolute -bottom-20 -left-20 w-40 h-40 bg-purple-500/20 rounded-full blur-3xl group-hover:bg-purple-500/30 transition-all duration-500"
            >
            </div>

            <div class="relative z-10">
                <h1
                    class="text-3xl font-bold text-center mb-2 bg-clip-text text-transparent bg-linear-to-r from-white to-slate-400"
                >
                    {t("login.welcome")}
                </h1>
                <p class="text-center text-slate-400 mb-8 text-sm">
                    {t("login.subtitle")}
                </p>

                {showInvalidCredentials && (
                    <p class="mb-6 rounded-lg border border-red-400/20 bg-red-400/10 px-4 py-3 text-center text-sm text-red-200">
                        {t("login.invalidCredentials")}
                    </p>
                )}

                <form action="/api/auth/signin" method="post" data-astro-reload class="space-y-6">
                    <div>
                        <label
                            for="email"
                            class="block text-xs font-medium text-slate-400 uppercase tracking-wider mb-2"
                            >{t("login.email")}</label
                        >
                        <input
                            type="email"
                            name="email"
                            id="email"
                            required
                            class="w-full bg-white/5 border border-white/10 rounded-lg px-4 py-3 text-white focus:outline-none focus:border-primary-500 focus:ring-1 focus:ring-primary-500 transition-all placeholder:text-slate-600"
                            placeholder="tu@email.com"
                        />
                    </div>

                    <div>
                        <label
                            for="password"
                            class="block text-xs font-medium text-slate-400 uppercase tracking-wider mb-2"
                            >{t("login.password")}</label
                        >
                        <input
                            type="password"
                            name="password"
                            id="password"
                            required
                            class="w-full bg-white/5 border border-white/10 rounded-lg px-4 py-3 text-white focus:outline-none focus:border-primary-500 focus:ring-1 focus:ring-primary-500 transition-all placeholder:text-slate-600"
                            placeholder="••••••••"
                        />
                    </div>

                    <button
                        type="submit"
                        class="w-full py-3 rounded-lg bg-linear-to-r from-primary-500 to-indigo-600 text-white font-bold shadow-lg shadow-primary-500/20 hover:shadow-primary-500/40 hover:scale-[1.02] transition-all duration-200"
                    >
                        {t("login.signIn")}
                    </button>
                </form>

                <div class="mt-5 text-center">
                    <a href="/signup" class="text-xs text-primary-400 hover:text-white transition-colors">
                        {t("login.createAccount")}
                    </a>
                </div>

                <div class="mt-7 flex items-center gap-3 text-[10px] uppercase tracking-[0.18em] text-slate-600">
                    <span class="h-px flex-1 bg-white/10"></span>
                    <span>{t("login.demoDivider")}</span>
                    <span class="h-px flex-1 bg-white/10"></span>
                </div>

                <a
                    href="/api/demo/start"
                    class="mt-4 flex items-center gap-3 rounded-xl border border-amber-400/20 bg-amber-400/5 px-4 py-3 text-left transition-all hover:border-amber-300/40 hover:bg-amber-400/10 focus:outline-none focus:ring-2 focus:ring-amber-300/40"
                >
                    <span class="flex h-9 w-9 shrink-0 items-center justify-center rounded-lg bg-amber-400/10 text-amber-300">
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                            <path d="M2 12s3.5-6 10-6 10 6 10 6-3.5 6-10 6S2 12 2 12Z" />
                            <circle cx="12" cy="12" r="2.5" />
                        </svg>
                    </span>
                    <span class="min-w-0 flex-1">
                        <span class="block text-sm font-semibold text-amber-100">{t("login.demoCta")}</span>
                        <span class="mt-0.5 block text-xs text-slate-500">{t("login.demoDescription")}</span>
                    </span>
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 shrink-0 text-amber-300" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                        <path d="M5 12h14" /><path d="m12 5 7 7-7 7" />
                    </svg>
                </a>
            </div>
        </div>
    </div>
</Layout>
